# Source-Controlled Remediation Closeout — 2026-07-30

## Closed work

| Area | Closure | Proof |
|---|---|---|
| Discovery | Created-date discovery, topic validation, daily budget, persisted cycle/candidate decisions, kill switch and token-required readiness | Deterministic GitHub HTTP worker-cycle test plus live GitHub run |
| Package risk | GitHub Dependency Graph SBOM, purl normalization, OSV batch matching and `MAL-*` critical finding semantics | Fixture test and live OSV query |
| Abuse control | Requester-level rate admission only trusts forwarded identity from configured proxy peers | Unit coverage and direct local request tests |
| Reliability | SQLite recovery/retry coverage, real readiness, and optional PostgreSQL report-mirror proof | Workspace tests and ephemeral PostgreSQL integration test |
| UX | Semantic combobox, explicit optional-source outage state, Chromium mobile navigation and queue-failure E2E, axe serious/critical gate | 62 frontend unit tests and 3 Playwright tests |
| Operations and SCA | Production compose/deploy readiness checks, deployment/runbook documentation, Rust and frontend vulnerability gates with a tracked React Router exception | Compose config, shell syntax check, CI configuration and `npm audit --audit-level=critical` |

The deploy gate is fail-closed for anonymous and token-authenticated GitHub
connectivity. `scripts/test_deploy_contract.sh` proves a failed token netcheck
cannot be converted into a successful deployment, and the public-edge probe
uses the current `/assets/js/app.js` bundle rather than a legacy compatibility
asset. It also proves recovery copies an existing runtime database from
`/data/trust.db` and that the production workflow retains the public Chromium
release gate.

## Reproducible validation

```bash
cd backend
cargo test --workspace --all-targets
cargo clippy --workspace --all-targets -- -D warnings
cargo fmt --all -- --check

cd ../frontend
npm run format:check
npm run lint
npm run test:coverage
npm run build
npm audit --omit=dev --audit-level=critical
npm run test:e2e
```

All commands above passed in this closeout. Rust tests intentionally skip token-dependent live checks and the PostgreSQL check unless their explicit credentials/URL are provided.

## External release gates

These require a real staging/production environment and cannot be proven from source control alone:

1. Run the documented dependency-outage, queue-restart, rollback, and alert-delivery drill with an operator owner.
2. Run Chromium plus an assistive-technology manual pass against the deployed origin.
3. Provide `GITHUB_TOKEN` and `TEST_DATABASE_URL` in the release environment to run the intentionally gated live checks.

## Current public-release evidence

`https://ai-supply-chain-trust.aibim.ai/api/v1/healthz` and the current
application asset return HTTP 200. The most recent public Chromium gate still
fails on the deployed home page with `aria-allowed-attr` (critical) and
`aria-prohibited-attr` (serious). The source tree contains the corresponding
combobox/decorative-icon fixes and passes the same local gate; therefore the
public site is serving an older frontend bundle. Deploying this worktree through
the production workflow is required before the release can be considered
complete.

The two high-severity React Router audit notices are tracked in `audit/dependency-exceptions.md`; the application is a Vite SPA and does not enable the affected RSC server paths. No critical production dependency finding remains untriaged.
